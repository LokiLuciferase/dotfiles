# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# i3-gaps
gaps inner 7

# swayfx
corner_radius 10
default_border none

for_window [class=".*"] border pixel 0

# manage scratchpad: start terminal
for_window [title="Scratchpad"] move scratchpad

# import environment variables - which default tools to use
font pango:Monospace,DejaVuSansMNerdFont 10
set $term_font_size 12
include ~/.config/sway/local.config

# user specific startup applications
# local startup applications
exec "$HOME/.config/i3/scripts/local/i3-startup-pre || true"

# connectivity
exec --no-startup-id "nm-applet"
exec --no-startup-id "blueman-applet"

# screen locking
exec --no-startup-id "xss-lock --transfer-sleep-lock -- $HOME/.config/i3/scripts/i3-lock-screen"
exec --no-startup-id "swayidle -w timeout 300 'swaylock' timeout 360 'swaymsg "output * dpms off"' resume 'swaymsg "output * dpms on"' before-sleep 'swaylock'

# display setup
exec --no-startup-id "$HOME/.config/arandr/default.sh ; swaybg -i $HOME/.local/share/backgrounds/default"
exec --no-startup-id "picom --daemon"
exec --no-startup-id "redshift"

# keyboard and mouse/touchpad setup
exec --no-startup-id "numlockx"

# starting misc applications
exec --no-startup-id "env RUST_LOG=off workstyle"
exec --no-startup-id "$HOME/.config/i3/scripts/i3-swap-focus"
exec --no-startup-id "$HOME/.config/i3/scripts/i3-warn-low-batt"
exec --no-startup-id "/usr/lib/nvidia-prime-applet/nvidia-prime"
exec --no-startup-id "syncthing"
exec --no-startup-id "keepassxc"
exec --no-startup-id "alacritty --title Scratchpad -o window.opacity=1 -o font.size=$term_font_size -e $HOME/.config/i3/scripts/i3-scratchpad-tmux"
exec --no-startup-id "dbus-update-activation-environment --systemd DISPLAY WAYLAND_DISPLAY SWAYSOCK"

# starting any .desktop files in $HOME/.config/autostart
exec --no-startup-id "dex $HOME/.config/autostart/*"

# local startup applications
exec "$HOME/.config/i3/scripts/local/i3-startup-post || true"

bindsym $mod+Tab exec pkill -USR1 -F "${XDG_RUNTIME_DIR}/swap_focus.pid"
bindsym Control+Mod1+l exec --no-startup-id "swaylock"

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Use brightnessctl to adjust screen brightness.
bindsym XF86MonBrightnessUp exec --no-startup-id brightnessctl s +5% && $refresh_i3status
bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl s 5%- && $refresh_i3status

# Enable further key bindings.
bindsym XF86HomePage exec --no-startup-id "firefox || brave-browser || sensible-browser"
bindsym XF86Mail exec --no-startup-id "thunderbird"
bindsym XF86Calculator exec --no-startup-id "gnome-calculator"
# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# define floating window placement
floating_maximum_size 1280 x 720
for_window [app_id="org.keepassxc.KeePassXC"] floating enable
for_window [app_id="zenity"] floating enable
for_window [title="Reminders"] floating enable
for_window [instance="blueman-manager"] floating enable
for_window [window_role="GtkFileChooserDialog"] floating enable
for_window [title="Welcome to PyCharm"] floating enable
for_window [title="win0"] floating enable  # for pycharm splash screen
for_window [instance="cheese"] floating enable

# explicitly disable floating
for_window [class="firefox"] floating disable

# start a terminal
bindsym $mod+Return exec "alacritty -o font.size=$term_font_size"

# start browser
bindsym $mod+b exec "firefox || brave-browser || sensible-browser"
bindsym $mod+Shift+b exec "firefox --private-window || brave-browser --incognito"

# screenshot
bindsym --release $mod+Print exec "FN=$(mktemp -u).png && grim $FN && wl-copy < $FN && rm -f $FN && notify-send 'Screenshot sent to clipboard.'"
bindsym --release $mod+Shift+Print exec "FN=$(mktemp -u).png && slurp | grim -g - $FN && wl-copy < $FN && rm -f $FN && notify-send 'Screenshot sent to clipboard.'"

# kill focused window
bindsym $mod+Shift+q kill

# split into placeholder swallowing the next window
bindsym $mod+Shift+v split v, open append_layout ~/.config/i3/layouts/universal/placeholder.json
bindsym $mod+Shift+h split h, open append_layout ~/.config/i3/layouts/universal/placeholder.json

# start dmenu (a program launcher)
bindsym $mod+d exec --no-startup-id ~/.config/rofi/dmenu-launcher.sh drun || i3-dmenu-desktop --dmenu="dmenu -i -l 30"
bindsym $mod+shift+d exec --no-startup-id rofimoji --skin-tone neutral --typer wtype --selector-args '-theme ~/.config/rofi/launchers/type-2/style-2.rasi'
bindsym $mod+F1 exec --no-startup-id ~/.config/rofi/dmenu-launcher.sh window || i3-dmenu-desktop --dmenu="dmenu -i -l 30"
bindsym $mod+shift+s exec --no-startup-id ~/.config/rofi/dmenu-launcher.sh ssh || i3-dmenu-desktop --dmenu="dmenu -i -l 30"

# toggle session management menu
bindsym $mod+Shift+e exec --no-startup-id ~/.config/sway/scripts/sway-toggle-nagbar

# reload autorandr/xrandr config
bindsym $mod+Shift+x exec --no-startup-id autorandr --change

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+semicolon focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

set $window-move-amount 150px

# move focused window
bindsym $mod+Shift+j move left $window-move-amount
bindsym $mod+Shift+k move down $window-move-amount
bindsym $mod+Shift+l move up $window-move-amount
bindsym $mod+Shift+semicolon move right $window-move-amount

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left $window-move-amount
bindsym $mod+Shift+Down move down $window-move-amount
bindsym $mod+Shift+Up move up $window-move-amount
bindsym $mod+Shift+Right move right $window-move-amount

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# toggle split
bindsym $mod+BackSpace split toggle

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout toggle stacking split
bindsym $mod+w layout toggle tabbed split
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle
bindsym $mod+Shift+F floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad

# Show the first scratchpad window
bindsym $mod+minus scratchpad show

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+c reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# goto mark
bindsym $mod+g exec i3-input -F '[con_mark="%s"] focus' -l 2 -P 'Goto: '

# create mark
bindsym $mod+m exec i3-input -F 'mark %s' -l 2 -P 'Mark: '

# resize window (you can also use the mouse for that)
mode "resize" {
    # These bindings trigger as soon as you enter the resize mode

    # Pressing left will shrink the window’s width.
    # Pressing right will grow the window’s width.
    # Pressing up will shrink the window’s height.
    # Pressing down will grow the window’s height.
    bindsym j resize shrink width 10 px or 10 ppt
    bindsym k resize grow height 10 px or 10 ppt
    bindsym l resize shrink height 10 px or 10 ppt
    bindsym semicolon resize grow width 10 px or 10 ppt

    # same bindings, but for the arrow keys
    bindsym Left resize shrink width 10 px or 10 ppt
    bindsym Down resize grow height 10 px or 10 ppt
    bindsym Up resize shrink height 10 px or 10 ppt
    bindsym Right resize grow width 10 px or 10 ppt

    # back to normal: Enter or Escape or $mod+r
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

mode "swap" {
    # switch to workspace
    bindsym $mod+1 workspace $ws1
    bindsym $mod+2 workspace $ws2
    bindsym $mod+3 workspace $ws3
    bindsym $mod+4 workspace $ws4
    bindsym $mod+5 workspace $ws5
    bindsym $mod+6 workspace $ws6
    bindsym $mod+7 workspace $ws7
    bindsym $mod+8 workspace $ws8
    bindsym $mod+9 workspace $ws9
    bindsym $mod+0 workspace $ws10

    # change focus
    bindsym $mod+j focus left
    bindsym $mod+k focus down
    bindsym $mod+l focus up
    bindsym $mod+semicolon focus right

    # alternatively, you can use the cursor keys:
    bindsym $mod+Left focus left
    bindsym $mod+Down focus down
    bindsym $mod+Up focus up
    bindsym $mod+Right focus right

    # change focus (without mod)
    bindsym Left focus left
    bindsym Down focus down
    bindsym Up focus up
    bindsym Right focus right

    # enable targeting of existing marks
    bindsym $mod+g exec i3-input -F '[con_mark="%s"] focus' -l 2 -P 'Goto: '

    # enable targeting with focus switching
    bindsym $mod+Tab exec pkill -USR1 -F "${XDG_RUNTIME_DIR}/swap_focus.pid"

    bindsym Return swap container with mark "swapee"; unmark "swapee"; mode "default";
    bindsym Escape unmark "swapee"; mode "default";
}

bindsym $mod+Shift+i mark --add "swapee"; mode "swap"
# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)

# Load colors
include "~/.config/sway/themes/monsterkai.theme"

# set cursor theme
seat * xcursor_theme BreezeX-Black 32
exec --no-startup-id "gsettings set org.gnome.desktop.interface cursor-theme BreezeX-Black"
exec --no-startup-id "gsettings set org.gnome.desktop.interface cursor-size 32"

# define bar
bar {
    colors {
        separator          $bar_separator_colors
        background         $bar_background_colors
        statusline         $bar_statusline_colors
        focused_workspace  $bar_focused_workspace_colors
        active_workspace   $bar_active_workspace_colors
        inactive_workspace $bar_inactive_workspace_colors
        urgent_workspace   $bar_urgent_workspace_colors
    }
    font pango:Monospace,DejaVuSansMNerdFont bold 9
    position top
    tray_padding 0
    status_command "bumblebee-status || i3status"
}

input * {
    tap enabled
    natural_scroll enabled
}
input type:keyboard {
    xkb_layout us,at
    xkb_variant basic,nodeadkeys
    xkb_options grp:alt_space_toggle,caps:escape,eurosign:e,lv3:ralt_switch,altwin:menu_win
}
# other colors
client.focused          $focused_colors
client.focused_inactive $focused_inactive_colors
client.unfocused        $unfocused_colors
client.urgent           $urgent_colors
